{
  "private": true,
  "name": "bilnov",
  "version": "1.0.0",
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "db:migrate:prod": "pnpm --filter database prisma migrate deploy",
    "db:seed": "pnpm --filter database prisma db seed"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.4.0"
  }
}
