{
  "private": true,
  "name": "bilnov",
  "version": "1.0.0",
  "workspaces": [
    "apps/web",
    "packages/shared"
  ],
  "scripts": {
    "build": "npm run build --workspace=apps/web",
    "dev": "npm run dev --workspace=apps/web"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.4.0"
  }
}
